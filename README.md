# SentineIx
Mini compiler for a statically-typed language — lexer, recursive descent parser, semantic analyzer, TAC IR generator with 4 optimization passes, and tree-walking interpreter. Flask web IDE to inspect each compilation stage in real time.
